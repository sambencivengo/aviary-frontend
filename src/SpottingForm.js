import MapContainer from './MapContainer';
import { Wrapper } from '@googlemaps/react-wrapper';
const SpottingForm = () => {
	return (
		<>
			<h3>This is the component that will recieve "Spotting" input</h3>
			<MapContainer />
		</>
	);
};

export default SpottingForm;
